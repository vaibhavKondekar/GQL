-- SELECT statements
SELECT p.name, p.age
FROM MATCH (p:Person)
WHERE p.age > 25;

SELECT COUNT(*) AS total
FROM MATCH (p:Person);

SELECT p.city, COUNT(p) AS count
FROM MATCH (p:Person)
GROUP BY p.city
HAVING COUNT(p) > 10;

